const app = require("../app");
const debug = require("debug")("scrapers: server");
const http = require("http");

const port = 8000;

app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
});
